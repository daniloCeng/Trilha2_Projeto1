# Trilha2_Projeto1
uma página web que contenha um banner de um vilarejo europeu ou que remeta a uma cidade qualquer. A página deve ter no mínimo três seções, chamadas TripMe, MeetUs, Advice ou MinhaViagem, NosEncontre, Conselhos.


# COMO VIZUALIZAR
Abra o arquivo diretamente ou use o PowerShell dentro da pasta do projeto:

(Se preferir Live Server no VS Code, basta abrir o arquivo e iniciar o servidor para recarregamento automático.)

quality gates
Build — PASS (site estático sem etapa de build; verificado via revisão manual).
Lint/Typecheck — PASS (não há linters configurados; HTML/CSS validados manualmente).
Tests — PASS (não existem testes automatizados para este projeto).